name: First CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print message
        run: echo "🎉 CI/CD pipeline triggered successfully!"

      - name: Another step
        run: echo "Hello Ahtasham, your CI/CD is working!"
